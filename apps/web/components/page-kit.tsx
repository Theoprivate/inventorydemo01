"use client";
import type { ReactNode } from "react";
export function PageHeader({ eyebrow, title, description, actions }: { eyebrow?: string; title: string; description?: string; actions?: ReactNode }) { return <header className="mb-6 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div>{eyebrow && <p className="mb-1 text-xs font-black uppercase tracking-[.2em] text-red-600">{eyebrow}</p>}<h1 className="text-3xl font-black tracking-tight sm:text-4xl">{title}</h1>{description && <p className="mt-2 max-w-2xl text-sm text-zinc-600">{description}</p>}</div>{actions}</header>; }
export function ErrorBox({ error, retry }: { error: unknown; retry?: () => void }) { const code = process.env.NODE_ENV === "development" && error instanceof Error && "code" in error && typeof error.code === "string" ? error.code : ""; return <div className="border-l-4 border-red-600 bg-red-50 p-4 text-sm text-red-900"><p className="font-bold">{error instanceof Error ? error.message : "โหลดข้อมูลไม่สำเร็จ"}{code ? ` (${code})` : ""}</p>{retry && <button className="mt-2 underline" onClick={retry}>ลองใหม่</button>}</div>; }
export function Empty({ text }: { text: string }) { return <div className="border border-dashed border-zinc-400 bg-white p-8 text-center text-sm text-zinc-500">{text}</div>; }
export function Badge({ children, tone = "neutral" }: { children: ReactNode; tone?: "neutral" | "success" | "warning" | "danger" }) { const style = { neutral: "bg-zinc-200", success: "bg-emerald-100 text-emerald-900", warning: "bg-amber-100 text-amber-900", danger: "bg-red-100 text-red-900" }[tone]; return <span className={`inline-flex px-2 py-1 text-xs font-black ${style}`}>{children}</span>; }
