# Feature Map

Use this table before source reads. Paths were verified at commit `713ce1bb9c759fe697d673e3a6345f25da7553d8` plus the current item-upload working tree. API routes are under `/api/v1` unless noted.

| Feature | Frontend route → page/component | Backend route | Service | Repository | Google Sheets tabs | Status |
| --- | --- | --- | --- | --- | --- | --- |
| Login | `/login` → `apps/web/app/login/page.tsx`; shell session/logout → `apps/web/components/app-shell.tsx` | `POST /auth/login`, `POST /auth/logout`, `GET /auth/me` (also registered without prefix) | `apps/api/src/services/inventory-service.ts` (`login`) | `apps/api/src/repositories/google-sheets-inventory-repository.ts` (`findUserByUsername`, `updateLastLogin`) | `Users`, `Branches` | Implemented |
| Items | `/settings/items` → `apps/web/app/(protected)/settings/items/page.tsx`; create/edit/upload → `apps/web/components/item-config-form.tsx`, browser compression → `apps/web/lib/item-image-upload.ts`, pages → `apps/web/app/(protected)/settings/items/new/page.tsx`, `apps/web/app/(protected)/settings/items/[itemId]/edit/page.tsx` | `GET /items`, `POST /items`, `PATCH /items/:itemId`, `GET /categories`; Next `POST /api/uploads/items` → `apps/web/app/api/uploads/items/route.ts` | `apps/api/src/services/inventory-service.ts` (`items`, `saveItem`, `categories`); upload is handled by the Next route | `apps/api/src/repositories/google-sheets-inventory-repository.ts` (`read`, `upsert`); uploaded files → `apps/web/public/images/items/` | `Items`, `Categories` | Implemented |
| Store Items | `/settings/store-items` → `apps/web/app/(protected)/settings/store-items/page.tsx` | `GET /store-items`, `PUT /store-items/batch`, plus `GET /branches`, `/items`, `/categories`, `/locations` | `apps/api/src/services/inventory-service.ts` (`storeItems`, `saveStoreItems`, master reads) | `apps/api/src/repositories/google-sheets-inventory-repository.ts` (`read`, `upsert`) | `Store_Items`, `Branches`, `Items`, `Categories`, `Locations` | Implemented |
| Locations | `/settings/locations` → `apps/web/app/(protected)/settings/locations/page.tsx` | `GET /locations`, `POST /locations`, `PATCH /locations/:locationId` | `apps/api/src/services/inventory-service.ts` (`locations`, `saveLocation`) | `apps/api/src/repositories/google-sheets-inventory-repository.ts` (`read`, `upsert`) | `Locations` | Implemented |
| Stock Balance | `/inventory/balances` → `apps/web/app/(protected)/inventory/balances/page.tsx` | `GET /stock-balances`, `GET /stock-balances/:branchId/:locationId/:itemId`; owner rebuild has no current page: `POST /admin/rebuild-stock-balances` | `apps/api/src/services/inventory-service.ts` (`balances`, `rebuildBalances`) | `apps/api/src/repositories/google-sheets-inventory-repository.ts` (`read`, `clearAndWrite`) | `Stock_Balances`, `Stock_Movements`; display joins `Items`, `Store_Items`, `Locations`, `Categories` | Implemented; rebuild UI missing |
| Stock Movement | `/inventory/movements` → `apps/web/app/(protected)/inventory/movements/page.tsx`; UI helpers → `apps/web/components/stock-movement-ui.tsx`, `apps/web/lib/movement-workflow.ts` | `GET /stock-movements`, `POST /stock-movements` | `apps/api/src/services/inventory-service.ts` (`movements`, `createMovement`) | `apps/api/src/repositories/google-sheets-inventory-repository.ts` (`read`, `append`, `upsert`) | `Stock_Movements`, `Stock_Balances`, plus `Items`, `Locations`, `Categories` | Implemented |
| Stock Request | `/inventory/request` → `apps/web/app/(protected)/inventory/request/page.tsx`; cart → `apps/web/stores/backpack.ts` | `GET /requestable-items`, `POST /stock-requests` | `apps/api/src/services/inventory-service.ts` (`requestableItems`, `createRequest`) | `apps/api/src/repositories/google-sheets-inventory-repository.ts` (`createStockRequest`, `read`) | `Stock_Requests`, `Stock_Request_Items`, `Store_Items`, `Items`, `Categories`, `Stock_Balances` | Implemented |
| Request History/Cancel | `/inventory/requests` → `apps/web/app/(protected)/inventory/requests/page.tsx`; detail → `apps/web/app/(protected)/inventory/requests/[requestId]/page.tsx` | `GET /stock-requests`, `GET /stock-requests/:requestId`, `PATCH /stock-requests/:requestId`, `POST /stock-requests/:requestId/cancel` | `apps/api/src/services/inventory-service.ts` (`requests`, `requestDetail`, `cancelRequest`) | `apps/api/src/repositories/google-sheets-inventory-repository.ts` (`read`, `upsert`) | `Stock_Requests`, `Stock_Request_Items`, `Items`, `Users` | Implemented |
| Approval / Issue | `/inventory/stockroom` → `apps/web/app/(protected)/inventory/stockroom/page.tsx`; detailed actions → `apps/web/app/(protected)/inventory/requests/[requestId]/page.tsx` | `POST /stock-requests/:requestId/approve`, `/issue`, `/quick-issue`, `/reject` | `apps/api/src/services/inventory-service.ts` (`approveRequest`, `issueRequest`, `quickIssueRequest`, `rejectRequest`) | `apps/api/src/repositories/google-sheets-inventory-repository.ts` (`read`, `append`, `upsert`) | `Stock_Requests`, `Stock_Request_Items`, `Stock_Movements`, `Stock_Balances`, `Store_Items`, `Locations`, `Items`, `Users` | Implemented |
| Stock Count | `/inventory/count` → `apps/web/app/(protected)/inventory/count/page.tsx`; paper create/preview → `apps/web/app/(protected)/inventory/count/paper/page.tsx`; paper print-only → `apps/web/app/inventory/count/paper/print/page.tsx`; shared paper document → `apps/web/components/documents/stock-count-printable-document.tsx`; scan/review → `apps/web/app/(protected)/inventory/count/scan/page.tsx`; detail → `apps/web/app/(protected)/inventory/count/[countId]/page.tsx`; helpers → `apps/web/lib/stock-count-payload.ts`, `apps/web/lib/stock-count-paper.ts`; card → `apps/web/components/stock-count-card.tsx` | `POST /stock-counts`, `POST /stock-counts/paper`, `GET /stock-counts`, `GET /stock-counts/:countId`, `GET /stock-counts/:countId/print`, `POST /stock-counts/:countId/uploads`, `POST /stock-counts/:countId/ocr`, `GET /stock-counts/:countId/ocr-result`, `PATCH /stock-counts/:countId/items`, `POST /stock-counts/:countId/complete` | `apps/api/src/services/inventory-service.ts` (`createCount`, `createPaperCount`, `processCountOcr`, `updateCountItems`, `completeCount`, `counts`, `countDetail`) | `apps/api/src/repositories/google-sheets-inventory-repository.ts` (`read`, `append`, `upsert`) | `Stock_Counts`, `Stock_Count_Items`, `Stock_Movements`, `Stock_Balances`, `Store_Items`, `Items`, `Locations`, `Branches`, `Users` | Implemented with mock OCR adapter; scanned file bytes are not persisted |

## Shared flow files

- API route registration and validation: `apps/api/src/app.ts`
- Service rules/orchestration: `apps/api/src/services/inventory-service.ts`, with stock rules in `apps/api/src/services/rules.ts`
- Repository contract: `apps/api/src/repositories/inventory-repository.ts`
- Google Sheets adapter: `apps/api/src/repositories/google-sheets-inventory-repository.ts`
- Sheet models/headers and row mapping: `apps/api/src/models.ts`, `apps/api/src/utils/mappers.ts`
- Browser API wrapper/contracts: `apps/web/lib/api.ts`, `apps/web/lib/types.ts`

Activity/XP/KPI is intentionally absent from the main table: repository/service/schema pieces exist under `apps/api/src/modules/user-activity/` and shared models, but no current HTTP route is registered, so the feature is partial rather than an implemented user flow.
